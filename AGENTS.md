# AIvideo｜通用人设与分镜工作区

本仓库用于 AI 短剧、漫剧及叙事视频项目，核心产出是角色人设图提示词与 AI 视频分镜脚本。项目事实、默认画风和路径以根目录 `project.json` 为准；不得把示例、旧项目记忆或模型常识当成当前项目事实。

## 初始化状态

1. 开始任务前读取 `project.json`。
2. 当 `initialized` 为 `false` 时，把本仓库视为未命名母版：允许整理用户提供的新项目资料，但不得擅自补写世界观、角色或剧情。
3. 收到新项目的首份剧本或设定后，使用 `script-analyzer` 做最小必要提取，更新项目配置与数据库，再进入人设或分镜流程。
4. 发现影响剧情因果、角色身份、时间线或视觉连续性的严重冲突时暂停，先征求用户意见。

## 核心路径

- 项目配置：`project.json`
- 原始剧本：`项目资料/剧本/`
- 项目数据库：`项目资料/database/`
- 最新人设提示词：`项目资料/outputs/人设/人设提示词.md`
- 正式分镜：`项目资料/outputs/分镜/第X集_分镜脚本_*.md`
- 剪辑版候补镜头：`项目资料/outputs/分镜/第X集_*_剪辑版.md`
- 分镜连续性缓存：`memory/storyboard-cache/第X集.md`
- 项目记忆与状态：`memory/`
- 工作技能：`.agents/skills/`

## 技能路由

1. 角色、场景、生物、灵体和关键道具的视觉资产设定与生图提示词：使用 `ai-short-drama-character`。
2. 普通对白、情绪表演、多人调度、连续性、正式分镜与剪辑版：使用 `storyboard-director`。
3. 连续对攻、格挡、追逐、摔投、能力交锋或明确战斗设计：由 `storyboard-director` 划定剧情边界，再调用 `seedance-combat-prompt` 设计动作链。
4. 新剧本导入、基础错误检查、历史资产对比、数据库更新、设定缺口或严重冲突审查：使用 `script-analyzer`。
5. 单次能力展示、短促推搡和普通走位不启用完整战斗流程。

## 剧本轻量分析规则

1. 单纯检查错别字、角色名、语病或明显逻辑问题时，只读取当前剧本与必要的名称/别名目录；不加载完整场景库、历史分镜、参考图或无关 Skill。
2. 历史资产先按名称、ID 和别名匹配；只有疑似已有或需要确认细节时，才读取对应单条详细设定。
3. 场景匹配必须区分已有场景、已有场景子区域和新增场景；重要道具只覆盖剧情关键、跨镜持续、跨集复用、武器、特殊装置或强连续性物件。
4. 基础检查只报告明显错误，并区分必须修改、建议修改和仅供确认；没有明显问题时直接说明未发现需要修改的明显错误。
5. 巧思建议是可选项，只有能以小修改明显改善笑点、动作、反差、转场、伏笔或角色反应时才提出，不强行为每集凑建议。

## 数据库职责

- `角色.json`：角色身份、名称与别名、性格/OOC 限制、稳定声线、外观锚点、关系、能力、武器及状态变化。
- `场景.json`：名称与别名、空间结构、入口、动线、光源、关键标志物、固定/允许变化元素及可交互物。
- `生物与灵体.json`：非人角色、怪物、灵体、召唤物及能力载体。
- `道具与制度.json`：剧情关键物、武器、设备、规则和组织制度。
- `世界观.json`：只保存会约束人设、场景图或分镜的跨项目稳定规则，如时代地域、技术边界、能力体系、统一视觉规则与不可变规则；不保存单集事件、角色当前状态或具体场景布置。
- `分集剧情.json`：顶层 `项目总览` 保存一句话梗概、主线目标、主要矛盾与阶段节点；`集数` 保存逐集摘要、跨集连续性、角色/场景状态变化与处理状态。

剧情事实只保留一个权威位置：项目级主线归 `分集剧情.json.项目总览`，逐集事件与跨集状态归 `分集剧情.json.集数`，集内镜头连续性归 `memory/storyboard-cache/第X集.md`，不得在 `世界观.json` 中重复记录。

只更新任务需要的数据库，不做无关扩写。新增或变化内容必须有剧本或用户资料依据；不确定信息标记为“待确认”。

## 视觉提示词规则

1. 所有人设、场景、生物、灵体和关键道具的生图提示词固定输出两版：精简版与扩展版。
2. 精简版只包含 `project.json.default_visual_style`、必要制图规范，以及用户资料或剧本直接要求的主体、场景物品和动作；不得使用合理推断、数据库延伸或审美补全增加新元素。
3. 扩展版允许在不改变项目事实、固定识别锚点和剧情边界的前提下，按生成需要补充构图、空间层次、材质、光影、氛围和可复用细节。
4. 固定画风以 `project.json.default_visual_style` 和 `project.json.paths.style_references` 中的参考图为准；后续视觉提示词不得自行切换为写实、三维、厚涂或其他画风。
5. 每一条人设图、场景图、生物、灵体和关键道具的生图提示词，都必须以“生成4次，每次返回不同图片。”作为最后一句，不得省略、改写或在其后追加其他内容。
6. 生成场景图提示词时，必须在场景内容描述之后自动追加 `project.json.workflow.visual_prompt_output.scene_style_suffix`，再追加 `project.json.workflow.visual_prompt_output.required_suffix`；固定场景画风后缀之后不得插入新的场景内容，生成次数句之后不得追加任何文字。

## 人设规则

1. 锁定数据库与用户明确给出的不可变识别点，不自行改变发色、年龄感、体型、服装类别、能力源点、武器或关键遮挡物。
2. 按“视觉提示词规则”固定输出精简版与扩展版，不再生成第三个可选风格版。
3. 人形角色默认采用纯白背景、自然直立站姿、完整展示身体结构；用户明确要求其他姿势或用途时例外。
4. 不额外添加家具、环境摆件或无关道具。明确绑定的武器、能力载体和剧情关键物件可以保留。
5. 默认不单列 negative prompt；特殊避坑要求用中文自然语言说明。
6. 服装类型、时代风格与技术风格以 `project.json`、数据库和用户要求为准，不继承旧项目默认值。
7. `人设提示词.md` 只保留最新一批提示词，不累积历史版本。
8. 新增生物、灵体或重要道具只有在影响人设或分镜时才展开视觉提示词。
9. 已有正式声线或声线提示词必须复用；同一角色不按集重生成声线。声线未确认时标记“待确认”，不得伪造正式声线。
10. 上传角色参考图时只提取必须保持、允许变化和禁止变化的特征；上传场景参考图时优先提取空间结构、门窗通道、主体家具、活动区域与前中后景关系。已理解的图片与锚点在同一任务内复用，不重复分析。

## 分镜规则

1. 画面风格优先读取 `project.json.default_visual_style`；为空时先确认风格，不擅自套用旧项目画风。
2. 每次最多生成 4 个连续单元；用户指定更小范围时按指定范围执行。
3. 新集首次制作时读取一次该集剧本及必要资料，建立 `memory/storyboard-cache/第X集.md`；后续批次优先读取缓存与上一批末态。
4. 普通正式制作默认使用紧凑成片版；快速拆解使用快速版；复杂多人、连续战斗和高风险特效才升级为精细版。
5. 普通对白和过场镜头只保留服务剧情的核心动作；完整微表演链只用于重要台词、情绪转折、反应镜与悬念落点。
6. 三人及以上场景执行人数锁定、站位图谱、轴线锁定、景别降载、画外人员说明和接触动作邻接检查。
7. 战斗场景执行动作切片、力学传导、能力特效绑定、三维轨迹、惯用手/武器锁定和结果确认。
8. 剪辑版是与正常版节点和时长对齐的平行候补镜头，不是另一条时间线。
9. 不新增剧本外角色、台词、胜负、伤势、道具或剧情结果。
10. 每个单元先给出自然可演的预计时长范围，范围上限不得超过 15 秒；15 秒是上限，不要求把完整表演拆成碎动作。
11. 分镜输出模式支持“文字版”和“带图版”。默认文字版不调用生图；带图版先完成文字分镜，再复用本单元已读取的角色、场景、道具、参考图和锚点生成 1 张代表性预览图。
12. 带图版预览图用于判断景别、主机位、构图、站位、空间与主要动作，不宣称完整还原动态运镜，也不得为了预览图重新分析整集剧本或重新读取全部数据库和 Skill。

## 分镜效率规则

1. 同集续写默认采用增量上下文：只读取 `project.json`、连续性缓存的必要字段、上一批末态和本次剧本行；缓存信息足够时不得重读整集、完整数据库或已确认分镜。
2. `storyboard-director` 默认输出紧凑成片版。普通单元控制在 4-6 镜，每镜只写一个因果链；全局画风、人数、同模、体型、轴线和道具锁定集中写一次，不逐镜整段重复。
3. 分镜技能的参考资料默认不加载。只有用户明确要求精细表演、复杂反应、特殊运镜、剪辑候补或重新选风格时，才读取相应单份参考。
4. 连续战斗才调用 `seedance-combat-prompt`，并只传递剧情结果、时长、角色动作资产、初始站位、场地、终局和禁改项，不重复传入整集剧本或全部数据库。
5. 正常完成一个分镜单元时只写正式分镜并更新连续性缓存；数据库处理状态按阶段或批次更新，不为每个普通单元重复写入。
6. 输出前保留脚本校验，但不在回复中展开内部估时、调用单、自查清单或重复粘贴分镜全文；向用户提供结果摘要和文件链接即可。
7. 一个单元默认只读取当前剧情、当前出场人物的性格/OOC 与视觉摘要、当前场景必要结构、当前关键道具、上一单元末态和当前 Skill；同一任务中已经读取的内容直接复用。
8. QA 默认只检查当前分镜与本集必要状态；只有发现疑似矛盾时才回查原剧本或对应资产条目，不为 QA 重新扫描全项目。

## 新项目首轮流程

项目资料进入仓库后按以下顺序执行：

1. 确认项目名称、题材、默认视觉风格、目标图像模型和视频模型。
2. 保存原始资料，不覆盖用户源文件。
3. 做严重问题审查与最小资产提取。
4. 更新项目配置、空数据库和项目状态。
5. 先制作一个主要角色人设与一个分镜单元作为校准样稿。
6. 用户确认风格与格式后，再批量推进。

## 工作边界

- 默认不生成营销文案、无关世界观扩写、非必要场景图、非必要道具图或泛化教程。
- 用户只要求分析、解释或审查时，不写入数据库或产出文件，除非用户同时要求保存或更新。
- 修改已有分镜时，只改动用户指出的单元或镜号，不无故重写已确认内容。
