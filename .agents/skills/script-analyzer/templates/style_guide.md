# 项目视觉规则模板

视觉规则优先级：用户本次明确要求 > `project.json` > 已确认项目数据库 > 当前剧本明确描述。

需要确认并记录：

- 默认画面风格与渲染媒介。
- 时代、地域、服装类别和材质边界。
- 色彩、光影、画幅和分辨率。
- 角色是否需要纯白背景标准人设图。
- 能力、武器、生物和道具的视觉表现边界。
- 明确禁止的视觉元素。

禁止从旧项目、示例或类型片惯例中自动继承规则。默认不单列 negative prompt。
